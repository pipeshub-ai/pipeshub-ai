name: Gemini Code Review on Push

# pull_request_target runs in base branch context with write access
# unlike pull_request which has restricted GITHUB_TOKEN permissions
on:
  pull_request_target:
    types: [synchronize]
    branches:
      - main

permissions:
  pull-requests: write
  issues: write

jobs:
  trigger-gemini-review:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Gemini Code Assist review
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.payload.pull_request.number;
            const commitSha = context.payload.pull_request.head.sha;
            const shortSha = commitSha.substring(0, 7);

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              body: `/review\n\n_Auto-triggered by push ${shortSha}_`
            });

            console.log(`Triggered Gemini review for PR #${prNumber} on commit ${shortSha}`);
