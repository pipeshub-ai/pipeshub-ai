{
  "name": "pipeshub-cli",
  "version": "0.1.0",
  "description": "Pipeshub local sync CLI — authenticate and configure Folder Sync",
  "license": "MIT",
  "author": "Pipeshub",
  "keywords": ["pipeshub", "cli", "sync"],
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "pipeshub": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "dotenv": "^16.4.7",
    "fernet": "^0.3.3",
    "keytar": "^7.9.0",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/prompts": "^2.4.9",
    "typescript": "^5.7.2"
  }
}